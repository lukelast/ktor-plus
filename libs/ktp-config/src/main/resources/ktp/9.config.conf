app {
  name = ""
  nameShort = ""
  version = "local"
  version = ${?KUBE_APP_VERSION}
  version = ${?K_REVISION}
  // An app wide secret for encryption tasks.
  secret = ""
  hostname = "unknown"
  hostname = ${?USER}
  hostname = ${?USERNAME}
  hostname = ${?HOSTNAME}
  server {
    port = 8080
    host = "0.0.0.0"
  }
}
