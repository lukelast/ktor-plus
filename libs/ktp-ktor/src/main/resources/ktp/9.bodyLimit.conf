bodyLimit {
  # Largest request body; larger gets 413. A route overrides it for itself and its children with
  # `bodyLimit(bytes)`. Cloud Run caps requests at 32 MiB regardless.
  default = 16 MiB
}
